template(name="palace_index")
  h1 Friends

  table.table
    each my_friends
      tr
        td.name
          a(href="/palace/#{_id}")
            | =name
        td.date
          a(href="/palace/#{_id}")
            | {{formatDate date}}

  hr
  table.table
    each all_friends
      tr
        td.name
          a(href="/palace/#{_id}")
            | =name
        td.date
          a(href="/palace/#{_id}")
              | {{formatDate date}}

  hr
  h2 Add a friend
  +friend_form
